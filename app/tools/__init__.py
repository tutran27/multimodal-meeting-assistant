"""
Package: app.tools
Vai trò: Tầng cung cấp các công cụ tiện ích (Tools) cho hệ thống Multi-modal Smart Personal Assistant.

Bao gồm các công cụ:
- `audio_stt`: Chuyển đổi âm thanh cuộc họp thành văn bản (Speech-to-Text).
- `image_ocr`: Nhận diện văn bản từ hình ảnh/tài liệu scan (OCR).
- `script_parser`: Đọc và phân tích cấu trúc tài liệu/kịch bản cuộc họp (Word, PDF, TXT).
- `fact_validator`: Kiểm tra và đối soát tính chính xác của dữ liệu trích xuất (email, deadline, evidence).
- `google_calendar`: Tra cứu lịch rảnh và tạo sự kiện trên Google Calendar.
- `gmail_draft`: Tạo bản thảo email đính kèm báo cáo trên Gmail.
- `web_search`: Tìm kiếm thông tin mở rộng trên web qua Tavily Search API.
- `pdf_generator`: Tạo báo cáo tổng hợp cuộc họp định dạng PDF (ReportLab).
"""
